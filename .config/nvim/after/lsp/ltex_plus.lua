return {
    settings = {
        ltex = {
            enabled = false, -- disabled by default (enable with with command in lsp.lua)
            language = "en-US", -- default to english
            -- language = "de-DE", -- change language via command (see lsp.lua)
        }
    }
}
